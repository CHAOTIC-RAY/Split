{
  "name": "split-web-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker serving the Spilt web application",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
